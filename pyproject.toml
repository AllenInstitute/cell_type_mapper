[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name="cell_type_mapper"
version="1.7.3"
dependencies = [
    "abc_atlas_access @ git+https://github.com/AllenInstitute/abc_atlas_access",
    "anndata>=0.11.0",
    "argschema",
    "matplotlib",
    "marshmallow==3.12.2",
    "mmc_gene_mapper @ git+https://github.com/AllenInstitute/mmc_gene_mapper@v0.2.2",
    "numpy",
    "pytest",
    "pytest-xdist",
    "pyzmq",
    "scipy"
]
requires-python = ">=3.10"
authors = [
    {name = "Scott Daniel", email = "scott.daniel@alleninstitue.org"},
]
